{
    "builds": [
      {
        "src": "./index.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      { "src": "/api", "dest": "/" },
      { "src": "/users", "dest": "/" },
      { "src": "/api(.*)", "dest": "/" }
    ]
  }